# 前端直连开发说明：
# 正常使用仓库根目录的 .env，由 scripts/dev.sh 统一加载。
# 若单独运行 `npm run dev`，可复制本文件为 .env.local 覆盖默认值。
# 注意：Vite 只会把 VITE_ 前缀变量暴露给浏览器代码，端口用 FRONTEND_PORT 在此不生效，
# 端口/代理目标的缺省值见 vite.config.ts。
VITE_API_BASE=/api
